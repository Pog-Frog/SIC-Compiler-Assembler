The input1 and the input2 Files are to test the assembler.py
to change the file that you want to run in the compiler open the lexical_analyzer.py and change the file name, you will find the in the 1st line 
the .asm files are the output of the compiler
to run the compiler only run the generator.py file only