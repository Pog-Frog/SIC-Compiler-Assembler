1.The input1 and the input2 Files are to test the assembler.py
2.to change the file that you want to run in the compiler open the lexical_analyzer.py and change the file name, you will find the in the 1st line 
3.the .asm files are the output of the compiler
4.to run the compiler only run the generator.py file only